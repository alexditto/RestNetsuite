/vendor/
composer.lock
.phpunit.result.cache
.phpunit.cache/
.DS_Store
.idea/
.vscode/
.env
CLAUDE.md
netsuite-client-project-outline.md

# Real tests against a live NetSuite sandbox account. Never committed — see
# tests/Sandbox/README.md. The harness (SandboxTestCase, bootstrap, this README) stays
# tracked; only the actual test files (which touch real sandbox data) are excluded.
/tests/Sandbox/Tests/